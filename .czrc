{
  "path": "cz-emoji",
  "config": {
    "cz-emoji": {
      "conventional": true,
      "symbol": false,
      "scopes": [
        "deps",
        "build",
        "release",
        "note",
        "label",
        "auth"
      ]
    }
  }
}
